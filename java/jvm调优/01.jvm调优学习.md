1. #### 开始认识

1. #### 类加载机制
   - 双亲委派：向上委托查找，向下委托加载。类加载体系：自定义类加载器 -> 应用类加载器 -> 扩展类加载器（jdk9以后基本没有了，换成了平台类加载器） -> 启动类加载器（JDK核心）。
         SPI打破双亲委派的必要性。
   - 类缓存：每个类加载器对他加载过的类都有一个缓存
   - 沙箱保护机制：不允许应用程序加载JDK内部的系统类

3. #### 啊
